[project]
name = "qwenmodern"
version = "0.1.0"
description = "Qwen chatbot interface"
requires-python = ">=3.11"
dependencies = [
    "dashscope",
    "gradio",
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "api/index.py"
